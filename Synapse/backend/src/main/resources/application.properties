spring.application.name=backend

spring.data.mongodb.uri=${SPRING_DATA_MONGODB_URI:mongodb://localhost:27017/synapse_db}

# JWT Configuration
# IMPORTANT: In production, set JWT_SECRET environment variable to a secure 256-bit key
jwt.secret=${JWT_SECRET:synapse-dev-secret-key-must-be-at-least-32-characters-long}
jwt.expiration=36000000
